import { createFileRoute } from "@tanstack/react-router";
import { CheckCircle2, CircleDashed } from "lucide-react";
export const Route = createFileRoute("/system")({ component: System });
function System() { const rows = [["FREE RESULTS FEED","Public football results/fixtures","READY"],["LOCAL STATISTICAL ENGINE","Form + scoring model","ACTIVE"],["WEATHER","Open-Meteo adapter slot","READY"],["NEWS / LINEUPS","Optional evidence adapters","NOT CONNECTED"],["ODDS","Optional market adapter","NOT CONNECTED"],["AI SYNTHESIS","Optional reasoning layer","NOT REQUIRED"]]; return <div className="mx-auto max-w-6xl px-5 py-10 lg:px-10"><div className="label-xs text-primary">SYSTEM HEALTH</div><h1 className="mt-3 text-3xl font-semibold">Provider and model status</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">The application is intentionally dependency-light. The core match intelligence can run without paid API credentials.</p><div className="panel mt-8 overflow-hidden">{rows.map(([name,detail,status]) => <div key={name} className="flex items-center gap-4 border-b border-border p-5 last:border-0"><div>{status === "ACTIVE" || status === "READY" ? <CheckCircle2 className="size-5 text-positive" /> : <CircleDashed className="size-5 text-muted-foreground" />}</div><div className="min-w-0 flex-1"><div className="font-medium">{name}</div><div className="text-sm text-muted-foreground">{detail}</div></div><span className="label-xs">{status}</span></div>)}</div></div> }
