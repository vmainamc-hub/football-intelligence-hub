import { createFileRoute } from "@tanstack/react-router";
import { ModulePage } from "@/components/gfi/module-page";
export const Route = createFileRoute("/engines")({ component: () => <ModulePage title="Engine Arena" eyebrow="MODEL COMPARISON" description="Inspect each model independently before a consensus layer combines them. Disagreement is retained as information, not hidden." stages={[{name:"Goal-rate engine",detail:"Current free-mode Poisson-style scoring model.",live:true},{name:"Form engine",detail:"Recent results and venue performance contribute to team strength.",live:true},{name:"xG / chance quality",detail:"Provider adapter slot reserved; no invented xG values.",live:false},{name:"Market divergence",detail:"Compare internal probability with independent market prices when available.",live:false}]} /> });
