import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { AlertTriangle, RefreshCw, Save } from "lucide-react";
import { Link } from "@tanstack/react-router";
import { runBatchAnalysis, type BatchAnalysisRow } from "@/lib/gfi/batch-analysis";
import { saveAuthoritativePrediction } from "@/lib/gfi/workbench";

const pct = (n:number) => `${(n*100).toFixed(1)}%`;

export function BatchLab(){
  const [limit,setLimit] = useState(20);
  const [selected,setSelected] = useState<BatchAnalysisRow | null>(null);
  const q = useQuery({queryKey:["authoritative-batch",limit],queryFn:()=>runBatchAnalysis({data:{limit,includeUpcoming:true}}),staleTime:5*60_000});
  if(q.isLoading) return <Shell><div className="panel p-8 text-sm text-muted-foreground">Scanning the global fixture fabric and running the authoritative pipeline…</div></Shell>;
  if(q.error) return <Shell><div className="panel p-8"><AlertTriangle className="size-5 text-warning"/><p className="mt-2 text-sm">Batch intelligence failed to run.</p><p className="mt-2 text-xs text-muted-foreground">{String(q.error)}</p><button onClick={()=>q.refetch()} className="mt-4 inline-flex items-center gap-2 rounded border border-border px-3 py-2 text-sm"><RefreshCw className="size-4"/> Retry scan</button></div></Shell>;
  const rows=q.data??[];
  return <Shell><div className="mb-5 flex flex-wrap items-center gap-2"><span className="label-xs">UPCOMING GLOBAL SCAN</span>{[10,20,40].map(n=><button key={n} onClick={()=>setLimit(n)} className={`rounded border px-3 py-1.5 text-xs ${limit===n?'border-primary text-primary':'border-border'}`}>{n}</button>)}<button onClick={()=>q.refetch()} className="ml-auto rounded border border-border p-2"><RefreshCw className="size-4"/></button></div><div className="grid gap-5 lg:grid-cols-[1fr_380px]"><div className="panel overflow-hidden"><div className="grid grid-cols-[40px_1fr_80px_80px_90px] border-b border-border px-4 py-3 label-xs"><span>#</span><span>MATCH</span><span>EDGE</span><span>CONF</span><span>QUALITY</span></div>{rows.map((r,i)=><button key={`${r.fixture.date}-${r.fixture.home}-${r.fixture.away}-${r.fixture.sourceId??i}`} onClick={()=>setSelected(r)} className={`grid w-full grid-cols-[40px_1fr_80px_80px_90px] border-b border-border px-4 py-4 text-left hover:bg-muted/30 ${selected===r?'bg-muted/40':''}`}><span className="metric">{i+1}</span><span><b>{r.fixture.home}</b> <span className="text-muted-foreground">vs</span> <b>{r.fixture.away}</b><small className="mt-1 block text-muted-foreground">{r.fixture.league} · {r.fixture.date}</small></span><span className="label-xs">{r.analysis.decision.replaceAll("_"," ")}</span><span className="metric">{r.analysis.confidence}</span><span className="metric">{r.analysis.quality}</span></button>)}</div><div className="panel p-5">{selected?<><div className="label-xs">AUTHORITATIVE ANALYSIS</div><h2 className="mt-2 font-semibold">{selected.fixture.home} vs {selected.fixture.away}</h2><div className="mt-1 text-xs text-muted-foreground">{selected.fixture.league} · source {selected.fixture.source??"unknown"}</div><div className="mt-4 grid grid-cols-3 gap-2">{([["H",selected.analysis.probabilities.home],["D",selected.analysis.probabilities.draw],["A",selected.analysis.probabilities.away]] as const).map(([k,v])=><div key={k} className="border border-border p-3"><div className="label-xs">{k}</div><div className="metric mt-1">{pct(v)}</div></div>)}</div><div className="mt-4 text-sm">{selected.analysis.finalPrediction}</div><div className="mt-2 text-xs text-muted-foreground">Robustness {selected.analysis.robustness.label} {selected.analysis.robustness.score}% · risk {selected.analysis.risk} · {selected.analysis.pipeline.evidenceMode}</div><div className="mt-4 border-t border-border pt-4 text-xs text-muted-foreground">{selected.analysis.pipeline.historicalRowsLoaded} historical rows · {selected.analysis.pipeline.homeSample}/{selected.analysis.pipeline.awaySample} team samples · {selected.analysis.engines.length} engines including simulation</div><button onClick={()=>saveAuthoritativePrediction(selected.analysis,selected.fixture)} className="mt-4 inline-flex items-center gap-2 rounded bg-primary px-3 py-2 text-sm text-primary-foreground"><Save className="size-4"/> Save prediction</button></>:<p className="text-sm text-muted-foreground">Select a match to inspect its complete decision trace.</p>}</div></div></Shell>
}

function Shell({children}:{children:React.ReactNode}){return <div className="mx-auto max-w-7xl px-5 py-8 lg:px-10"><Link to="/" className="label-xs">← HOME</Link><header className="mt-7 border-b border-border pb-6"><div className="label-xs text-primary">PORTFOLIO INTELLIGENCE</div><h1 className="mt-2 text-3xl font-semibold">Batch Intelligence</h1><p className="mt-2 max-w-2xl text-sm text-muted-foreground">One server-side pipeline scans upcoming fixtures, runs every available engine, validates through simulation, and ranks the strongest surviving edges.</p></header><div className="mt-6">{children}</div></div>}
