import { useMutation, useQuery } from "@tanstack/react-query";
import { Link } from "@tanstack/react-router";
import { AlertTriangle, CheckCircle2, Database, RefreshCw, Globe2 } from "lucide-react";
import { runBatchAnalysis } from "@/lib/gfi/batch-analysis";
import { inspectSystem } from "@/lib/gfi/source-registry";
import { reservoirStats, syncReservoir } from "@/lib/gfi/data-reservoir";
import { discoverOpenFootballUniverse, harvestOpenFootballUniverse } from "@/lib/gfi/global-openfootball";

export function SystemHealth() {
  const pipeline = useQuery({ queryKey: ["system-health", "pipeline"], queryFn: () => runBatchAnalysis({ data: { limit: 1, includeUpcoming: true } }), staleTime: 2 * 60_000 });
  const sources = useQuery({ queryKey: ["system-health", "sources"], queryFn: inspectSystem, staleTime: 2 * 60_000 });
  const reservoir = useQuery({ queryKey: ["system-health", "reservoir"], queryFn: reservoirStats, staleTime: 2 * 60_000 });
  const discovery = useQuery({ queryKey: ["system-health", "global-discovery"], queryFn: discoverOpenFootballUniverse, staleTime: 30 * 60_000 });
  const harvest = useMutation({ mutationFn: async () => { const result = await harvestOpenFootballUniverse({ data: { limit: 120 } }); return syncReservoir({ data: { rows: result.matches, dataset: "openfootball-global-harvest" } }); }, onSuccess: () => { reservoir.refetch(); sources.refetch(); } });
  const row = pipeline.data?.[0];
  const refresh = () => { pipeline.refetch(); sources.refetch(); reservoir.refetch(); discovery.refetch(); };
  return <div className="mx-auto max-w-7xl px-5 py-8 lg:px-10"><Link to="/" className="label-xs">← HOME</Link><header className="mt-7 border-b border-border pb-6"><div className="label-xs text-primary">SYSTEM HEALTH</div><h1 className="mt-2 text-3xl font-semibold">Operational status</h1><p className="mt-2 max-w-3xl text-sm text-muted-foreground">Runtime health is measured from production services and the persistent football reservoir. The global discovery fabric can enumerate public OpenFootball datasets and ingest them into the reservoir.</p></header><div className="mt-6 grid gap-5 lg:grid-cols-[1fr_420px]"><div className="panel p-5"><div className="flex items-center justify-between"><div className="label-xs">LIVE PIPELINE CHECK</div><button onClick={refresh} className="rounded border border-border p-2"><RefreshCw className="size-4"/></button></div>{pipeline.isLoading&&<div className="mt-5 text-sm text-muted-foreground">Running live pipeline health check…</div>}{pipeline.error&&<div className="mt-5 flex gap-3 text-sm"><AlertTriangle className="size-5 text-warning"/> Backend pipeline unavailable: {String(pipeline.error)}</div>}{row&&<div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[["FIXTURE","READY"],["ENGINES",String(row.analysis.engines.length)],["HISTORY",String(row.analysis.pipeline.historicalRowsLoaded)],["SIMULATION",row.analysis.pipeline.simulation.iterations.toLocaleString()+" runs"],["SOURCE",row.analysis.pipeline.source],["COMPETITION GROUPS",String(row.analysis.pipeline.competitionsLoaded)],["QUALITY",String(row.analysis.quality)],["EVIDENCE",row.analysis.pipeline.evidenceMode]].map(([k,v])=><div key={k} className="border border-border p-3"><div className="label-xs">{k}</div><div className="mt-1 text-sm font-semibold">{v}</div></div>)}</div>}</div><div className="panel p-5"><div className="flex items-center gap-2"><Database className="size-4 text-primary"/><div className="label-xs">PERSISTENT RESERVOIR</div></div>{reservoir.isLoading&&<div className="mt-4 text-xs text-muted-foreground">Checking cloud football memory…</div>}{reservoir.data&&!reservoir.data.configured&&<div className="mt-4 text-sm text-warning">Persistent storage is not configured on this deployment.</div>}{reservoir.data?.configured&&<div className="mt-4 grid grid-cols-2 gap-3">{[["TEAMS",reservoir.data.teams],["COMPETITIONS",reservoir.data.competitions],["MATCHES",reservoir.data.matches],["OBSERVATIONS",reservoir.data.observations]].map(([k,v])=><div key={String(k)} className="border border-border p-3"><div className="label-xs">{k}</div><div className="metric mt-1">{v}</div></div>)}</div>}{reservoir.data?.lastIngest&&<div className="mt-3 text-xs text-muted-foreground">Last ingestion: {new Date(reservoir.data.lastIngest).toLocaleString()}</div>}</div></div><div className="panel mt-5 p-5"><div className="flex items-center justify-between gap-4"><div><div className="label-xs">GLOBAL DATA HARVEST</div><div className="mt-2 text-sm font-medium">OpenFootball public universe discovery</div><div className="mt-1 text-xs text-muted-foreground">{discovery.isLoading ? "Discovering public datasets…" : `${discovery.data?.length ?? 0} dataset files discovered across the OpenFootball organization.`}</div></div><button disabled={harvest.isPending||!discovery.data?.length} onClick={()=>harvest.mutate()} className="inline-flex items-center gap-2 rounded bg-primary px-3 py-2 text-xs text-primary-foreground"><Globe2 className="size-4"/>{harvest.isPending?"Harvesting…":"Harvest global data"}</button></div>{harvest.data&&<div className="mt-4 text-xs text-muted-foreground">Harvest completed: {harvest.data.inserted.toLocaleString()} rows written/updated in the reservoir.</div>}{harvest.error&&<div className="mt-4 text-xs text-warning">Global harvest error: {String(harvest.error)}</div>}</div><div className="panel mt-5 p-5"><div className="label-xs">SOURCE FABRIC</div>{(sources.data??[]).map((s)=><div key={s.name} className="mt-3 flex items-start gap-3 border-b border-border pb-3 last:border-0"><CheckCircle2 className={`mt-0.5 size-4 ${s.status === "CONNECTED" ? "text-positive" : s.status === "FAILED" ? "text-warning" : "text-muted-foreground"}`}/><div className="min-w-0 flex-1"><div className="text-sm font-medium">{s.name}</div><div className="text-xs text-muted-foreground">{s.role}{typeof s.observedRows === "number" ? ` · ${s.observedRows.toLocaleString()} rows observed` : ""}</div></div><span className="label-xs">{s.status}</span></div>)}</div><div className="panel mt-5 p-5"><div className="label-xs text-primary">ARCHITECTURE CHECK</div><p className="mt-2 text-sm leading-6 text-muted-foreground">Match, Batch, Engine Arena, Evidence, Simulation and Model Lab share the authoritative graph. Searches combine persistent reservoir history with live source discovery. Advanced engines run on the same historical evidence package.</p></div></div>;
}
