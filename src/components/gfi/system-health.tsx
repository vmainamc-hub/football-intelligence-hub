import { useQuery } from "@tanstack/react-query";
import { Link } from "@tanstack/react-router";
import { AlertTriangle, CheckCircle2, RefreshCw } from "lucide-react";
import { runBatchAnalysis } from "@/lib/gfi/batch-analysis";
import { inspectSystem } from "@/lib/gfi/source-registry";

export function SystemHealth() {
  const pipeline = useQuery({ queryKey: ["system-health", "pipeline"], queryFn: () => runBatchAnalysis({ data: { limit: 1, includeUpcoming: true } }), staleTime: 2 * 60_000 });
  const sources = useQuery({ queryKey: ["system-health", "sources"], queryFn: inspectSystem, staleTime: 2 * 60_000 });
  const row = pipeline.data?.[0];
  const refresh = () => { pipeline.refetch(); sources.refetch(); };
  return <div className="mx-auto max-w-7xl px-5 py-8 lg:px-10"><Link to="/" className="label-xs">← HOME</Link><header className="mt-7 border-b border-border pb-6"><div className="label-xs text-primary">SYSTEM HEALTH</div><h1 className="mt-2 text-3xl font-semibold">Operational status</h1><p className="mt-2 max-w-3xl text-sm text-muted-foreground">Runtime health is measured from the same production services used by Match, Batch, Engine Arena, Evidence, Simulation and Model Lab.</p></header><div className="mt-6 grid gap-5 lg:grid-cols-[1fr_360px]"><div className="panel p-5"><div className="flex items-center justify-between"><div className="label-xs">LIVE PIPELINE CHECK</div><button onClick={refresh} className="rounded border border-border p-2"><RefreshCw className="size-4"/></button></div>{pipeline.isLoading&&<div className="mt-5 text-sm text-muted-foreground">Running live pipeline health check…</div>}{pipeline.error&&<div className="mt-5 flex gap-3 text-sm"><AlertTriangle className="size-5 text-warning"/> Backend pipeline unavailable: {String(pipeline.error)}</div>}{row&&<div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[["FIXTURE","READY"],["ENGINES",String(row.analysis.engines.length)],["HISTORY",String(row.analysis.pipeline.historicalRowsLoaded)],["SIMULATION",row.analysis.pipeline.simulation.iterations.toLocaleString()+" runs"],["SOURCE",row.analysis.pipeline.source],["COMPETITION GROUPS",String(row.analysis.pipeline.competitionsLoaded)],["QUALITY",String(row.analysis.quality)],["EVIDENCE",row.analysis.pipeline.evidenceMode]].map(([k,v])=><div key={k} className="border border-border p-3"><div className="label-xs">{k}</div><div className="mt-1 text-sm font-semibold">{v}</div></div>)}</div>}</div><div className="panel p-5"><div className="label-xs">SOURCE FABRIC</div>{sources.isLoading&&<div className="mt-4 text-xs text-muted-foreground">Checking adapters…</div>}{(sources.data??[]).map((s)=><div key={s.name} className="mt-3 flex items-start gap-3 border-b border-border pb-3 last:border-0"><CheckCircle2 className={`mt-0.5 size-4 ${s.status === "CONNECTED" ? "text-positive" : s.status === "FAILED" ? "text-warning" : "text-muted-foreground"}`}/><div className="min-w-0 flex-1"><div className="text-sm font-medium">{s.name}</div><div className="text-xs text-muted-foreground">{s.role}{typeof s.observedRows === "number" ? ` · ${s.observedRows.toLocaleString()} rows observed` : ""}</div></div><span className="label-xs">{s.status}</span></div>)}</div></div><div className="panel mt-5 p-5"><div className="label-xs text-primary">ARCHITECTURE CHECK</div><p className="mt-2 text-sm leading-6 text-muted-foreground">Match, Batch, Engine Arena, Evidence and Simulation consume the server-authoritative analysis graph. Prediction History and Post-Match Audit consume the same central prediction ledger. Model Lab reuses the same analysis graph chronologically. Legacy client-side analysis modules have been removed from the production route graph.</p></div></div>;
}
