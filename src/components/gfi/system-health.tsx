import { useQuery } from "@tanstack/react-query";
import { Link } from "@tanstack/react-router";
import { AlertTriangle, CheckCircle2, RefreshCw } from "lucide-react";
import { runBatchAnalysis } from "@/lib/gfi/batch-analysis";
import { UNIVERSAL_SOURCES } from "@/lib/gfi/universal-sources";

export function SystemHealth(){const q=useQuery({queryKey:["system-health"],queryFn:()=>runBatchAnalysis({data:{limit:1,includeUpcoming:true}}),staleTime:2*60_000});const row=q.data?.[0];const engineCount=row?.analysis.engines.length??0;return <div className="mx-auto max-w-7xl px-5 py-8 lg:px-10"><Link to="/" className="label-xs">← HOME</Link><header className="mt-7 border-b border-border pb-6"><div className="label-xs text-primary">SYSTEM HEALTH</div><h1 className="mt-2 text-3xl font-semibold">Operational status</h1><p className="mt-2 max-w-3xl text-sm text-muted-foreground">This page reports runtime-connected capabilities rather than static promises. A source is only marked connected when the application has an adapter for it; optional domains remain explicitly offline.</p></header><div className="mt-6 grid gap-5 lg:grid-cols-[1fr_360px]"><div className="panel p-5"><div className="flex items-center justify-between"><div className="label-xs">LIVE PIPELINE CHECK</div><button onClick={()=>q.refetch()} className="rounded border border-border p-2"><RefreshCw className="size-4"/></button></div>{q.isLoading&&<div className="mt-5 text-sm text-muted-foreground">Running live pipeline health check…</div>}{q.error&&<div className="mt-5 flex gap-3 text-sm"><AlertTriangle className="size-5 text-warning"/> Backend pipeline unavailable: {String(q.error)}</div>}{row&&<div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[['FIXTURE','READY'],['ENGINES',String(engineCount)],['HISTORY',String(row.analysis.pipeline.historicalRowsLoaded)],['SIMULATION',row.analysis.pipeline.simulation.iterations.toLocaleString()+' runs'],['SOURCE',row.analysis.pipeline.source],['COMPETITION GROUPS',String(row.analysis.pipeline.competitionsLoaded)],['QUALITY',String(row.analysis.quality)],['EVIDENCE',row.analysis.pipeline.evidenceMode]].map(([k,v])=><div key={k} className="border border-border p-3"><div className="label-xs">{k}</div><div className="mt-1 text-sm font-semibold">{v}</div></div>)}</div>}</div><div className="panel p-5"><div className="label-xs">SOURCE FABRIC</div>{UNIVERSAL_SOURCES.map(s=><div key={s.name} className="mt-3 flex items-start gap-3 border-b border-border pb-3 last:border-0"><CheckCircle2 className={`mt-0.5 size-4 ${s.configured?'text-positive':'text-muted-foreground'}`}/><div className="min-w-0 flex-1"><div className="text-sm font-medium">{s.name}</div><div className="text-xs text-muted-foreground">{s.role}</div></div><span className="label-xs">{s.configured?'ADAPTER':'OFFLINE'}</span></div>)}</div></div><div className="panel mt-5 p-5"><div className="label-xs text-primary">CURRENT GAP</div><p className="mt-2 text-sm leading-6 text-muted-foreground">StatArea predictions, live news, dedicated xG, confirmed lineups/injuries and bookmaker odds are not connected yet. The system should not fabricate them. They belong in the next external-intelligence layer.</p></div></div>}
